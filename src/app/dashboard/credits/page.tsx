import { CreditsClient } from './CreditsClient';

export const metadata = {
  title: 'API Credits | Dashboard',
  description: 'Buy and manage API credits',
};

export default function CreditsPage() {
  return <CreditsClient />;
}
